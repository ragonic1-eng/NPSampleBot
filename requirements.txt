python-telegram-bot[job-queue]==21.6
gspread==6.1.4
google-auth==2.35.0
rapidfuzz==3.10.1
python-dotenv==1.0.1
anthropic==0.39.0
httpx==0.27.2
requests==2.32.3
beautifulsoup4==4.12.3
pytesseract==0.3.13
Pillow==11.0.0
# RapidOCR: free local scene-text OCR (PP-OCR ONNX models). Much better
# than Tesseract at finding codes in angled/cluttered phone photos. If
# install ever causes trouble on Railway, delete these two lines (or set
# DISABLE_RAPIDOCR=1) — the bot falls back to Tesseract.
rapidocr==3.9.1
onnxruntime==1.20.1
# RapidOCR's transitive dep is the DESKTOP opencv-python, which links X11/GL
# libs a headless server (Railway) doesn't have — the cause of the recurring
# "libGL.so.1 / libxcb.so.1 not found" failures. Pin the headless build (same
# cv2 API, no GUI deps) so it's installed too; the nixpacks build phase then
# removes the desktop one, leaving cv2 = headless. Verified: RapidOCR reads
# fine with headless.
opencv-python-headless==5.0.0.93
